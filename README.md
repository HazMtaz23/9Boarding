# 9Boarding (fresh start)

Open `index.html` with the Live Server extension in VS Code (Right-click > Open with Live Server).

This is a single static page using Tailwind via CDN and the Inter font. The 9fin logo should be at `Images/9fin.jpg`.
